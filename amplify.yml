version: 1
frontend:
  phases:
    preBuild:
      commands:
        - bundle install
    build:
      commands:
        - rake site:build
  artifacts:
    baseDirectory: _site
    files:
      - '**/*'
  cache:
    paths: []

